logging:
  level:
    com.ricorich: debug
    org.springframwork: info
server:
  port: 80

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/springboot-test
    username: root
    password: 199418

mybatis:
  # 实体类别名包路径
  type-aliases-package: com.ricorich.pojo
  cconfiguration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl